import React from 'react';
import { Container } from './styles';

const MenuQueue: React.FC = () => (
  <Container>
    <div>
      <input />
      <button> Insert</button>
    </div>
    <button>Remove</button>
  </Container>
);

export default MenuQueue;
